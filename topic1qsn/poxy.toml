# poxy.toml
name = "Calendar Tool"
author = "Magpiny BO"
description = "A C++23 CLI tool for generating leap-year aware calendars."
license = "MIT"

# FIX 1: Theme must be a simple string at the root level.
# Valid values: "dark", "light", "custom"
theme = "dark"
dot = true

[sources]
paths           = [ 'calendar.cpp' ]
